# Spotisee

A way to see your spotify stats like never before (im so good at naming things right?)

To Run backend Server, run from `backend` folder
```commandline
mvn clean package
java -jar target/Spotisee-0.1.jar server src/main/resources/config.yml
```
To run in frontend, run from `frontend` folder
```commandline
npm run dev
```
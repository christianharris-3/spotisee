server:
  applicationConnectors:
    - type: http
      port: 8080

  adminConnectors:
    - type: http
      port: 8081

database:
  driverClass: com.mysql.cj.jdbc.Driver
  user: chris
  password: password
  url: jdbc:mysql://localhost:3306/Spotisee?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
  maxSize: 10

logging:
  level: INFO

  loggers:
    com.mycompany.app: INFO
    org.glassfish.jersey.server: DEBUG
    org.hibernate.SQL: DEBUG
    org.jdbi.v3.core.statement: DEBUG